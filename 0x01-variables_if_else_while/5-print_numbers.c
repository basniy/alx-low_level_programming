#include <stdlib.h>

/**
 * main - Entry point
 *
 * Description: Prints the lowercase alphabet excluding e and q
 * Return: 0
 */ 
int main(void)
{
char i;
for (i = '0'; i <= '0' ; ++i)
putchar('\n);(i)

putchar('\n);('\n);

return(0);
}
