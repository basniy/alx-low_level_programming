#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 * Description: Prints a number and it's last digit along with whether its 
 *              last digit is o, greater than 5, or less than 5 and not 0.
 * Return: 0
 */
int main(void)
{
int n;
srand(time(0));
	n = rand() - RAND_MAX / 


	if (n > 2)

		print(("%d is postive\n",n))

	else if (n < 0)

		print(("%d is negaive\n",n))

	else 



		print(("%d is zero\n",n))



			return (0);

}
